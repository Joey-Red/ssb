import { useMemo, useRef, useState, type KeyboardEvent } from 'react'
import { roster } from '../data/roster'
import { hrefFor } from '../router'
import './RosterView.css'

export function RosterView(){const[query,setQuery]=useState('');const refs=useRef<(HTMLAnchorElement|null)[]>([]);const normalized=query.trim().toLowerCase();const filtered=useMemo(()=>!normalized?roster:roster.filter((f)=>[f.name,f.series,...f.aliases].join(' ').toLowerCase().includes(normalized)),[normalized]);function onKey(event:KeyboardEvent<HTMLDivElement>){if(!['ArrowRight','ArrowLeft','ArrowDown','ArrowUp'].includes(event.key))return;const i=refs.current.findIndex((el)=>el===document.activeElement);if(i<0)return;event.preventDefault();const cols=window.innerWidth<560?1:window.innerWidth<900?2:window.innerWidth<1280?3:4;const delta=event.key==='ArrowRight'?1:event.key==='ArrowLeft'?-1:event.key==='ArrowDown'?cols:-cols;refs.current[Math.max(0,Math.min(filtered.length-1,i+delta))]?.focus()}return <div className="page-stack"><section className="hero-panel"><div><p className="eyebrow">SSBU training companion</p><h1>Pick a fighter. Practice the right thing.</h1><p className="hero-copy">Fast memory aids, source-aware combos, and a 0–200% training ladder. Frame data and move visuals will grow into the same fighter pages later.</p></div><div className="hero-stats" aria-label="Guide status"><div><strong>{roster.length}</strong><span>fighter pages</span></div><div><strong>4</strong><span>reference guides live</span></div><div><strong>0</strong><span>servers required</span></div></div></section><section className="panel roster-panel" aria-labelledby="roster-title"><div className="roster-toolbar"><div><p className="eyebrow">Roster</p><h2 id="roster-title">All fighters</h2></div><label className="search-field"><span aria-hidden="true">⌕</span><span className="sr-only">Search fighters</span><input type="search" value={query} onChange={(event)=>setQuery(event.target.value)} placeholder="Search fighter, alias, or series…"/>{query&&<button type="button" onClick={()=>setQuery('')} aria-label="Clear search">×</button>}</label></div><p className="result-count" aria-live="polite">{filtered.length} fighter{filtered.length===1?'':'s'}</p>{filtered.length===0?<div className="empty-state"><span className="empty-state__icon" aria-hidden="true">?</span><h3>No fighter found</h3><p>Try a fighter name, nickname, or series.</p></div>:<div className="roster-grid" onKeyDown={onKey}>{filtered.map((fighter,index)=><a key={fighter.id} className={`fighter-card${fighter.guideStatus==='ready'?' fighter-card--ready':''}`} href={hrefFor(`/fighter/${fighter.slug}`)} ref={(el)=>{refs.current[index]=el}}><div className="fighter-mark" aria-hidden="true">{fighter.name.split(/\s|&/).filter(Boolean).slice(0,2).map((part)=>part[0]).join('')}</div><div className="fighter-card__body"><span className="fighter-series">{fighter.series}</span><strong>{fighter.name}</strong><span className={`status-pill status-pill--${fighter.guideStatus}`}>{fighter.guideStatus==='ready'?'Guide ready':'Roster indexed'}</span></div><span className="fighter-arrow" aria-hidden="true">›</span></a>)}</div>}</section></div>}
